#include "/Engine/Private/Common.ush"

// Thx Temaran for original HLSL & usage example

RWTexture3D<float> ALightVolume;


[numthreads(1, 1, 1)]
void MainComputeShader(uint3 ThreadId : SV_DispatchThreadID)
{
    ALightVolume[ThreadId] = 0.0;
}
